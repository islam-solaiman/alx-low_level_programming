#include "main.h"

/**
 * add -  function that adds two integers and returns the result.
 * @x: first parametter
 * @y: second parametter
 * Return: sum of x and y
 */
int add(int x, int y)
{
	int z;

	z = x + y;

	return (z);
}
